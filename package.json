{
  "name": "rest-placeholders",
  "version": "1.0.0",
  "description": "",
  "main": "background.js",
  "scripts": {
    "start": "npm run css:watch",
    "css": "node-sass ./styles/styles.scss -o ./styles --output-style compressed",
    "css:watch": "npm run css && npm run css -- --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^4.13.0"
  }
}
